# basic configuration
backend = "glx";
vsync = true;

glx-copy-from-front = true;
glx-swap-method = 2;
xrender-sync-fence = true;

blur-background-frame = false;

wintypes:
{
dock = {
    shadow = true;
    full-shadow = true;
};

desktop = {
    shadow = true;
    full-shadow = true;
};


};
